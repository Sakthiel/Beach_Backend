spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/pulpticket
    username: sakthi
    password: sakthi

jpa:
  hibernate:
    ddl-auto: validate
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect